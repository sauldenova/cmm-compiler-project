/* int function(int a, int b) {
    if (a == b) {
        return a;
    } else {
        return b;
    }
} */

int main() {
    int a; int b; int c; int d;
    a = readInt();
    b = readInt();
    c = 1;
    //c = function(a, b);
    d = 1;
    while (c < d) {
        d = d + 1;
    }

    for (d = 1; c < d; d = d + 1) {
    }

    print(a, " ", b, " ", c, "\n");

    return 0;
}
