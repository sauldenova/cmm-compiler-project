/* int function(int a, int b) {
    if (a == b) {
        return a;
    } else {
        return b;
    }
} */

int main() {
    int a; int b; int c; int d;
    a = readInt();
    b = readInt();
    //c = function(a, b);
    d = 1;
    while (c < d) {
        d = d + 1;
    }

    for (d = 1; c < d; d = d + 1) {
    }

    printInt(a);
    printInt(b);
    printInt(c);

    return 0;
}
